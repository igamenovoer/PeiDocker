# Project Files Template

This directory contains the template files that are copied to a new PeiDocker project directory during initialization.

## Structure

*   `installation/`: Directory copied into the Docker image as `/pei-from-host`.
*   `*.Dockerfile`: Dockerfile templates for Stage 1 and Stage 2.
